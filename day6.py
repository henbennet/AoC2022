import re
import aocMain

def Parse(inputlines):

def Move1(par):
    return 0

def Move2(par):
    return 0

def main(inputlines):
    input = Parse(inputlines)
    pattern = r'\d+'

    for line in inputlines:
        if re.search(pattern, line) is not None:

            instances = re.findall(pattern, line)
    part1 = 0
    part2 = 0
    return part1, part2

aocMain.runLines(main, "day6.txt")
